import Schema from '../../../containers/Schema'

const schemaRoute = {
  key: 'schema',
  path: 'schemas/:schemaId',
  label: 'メッセージスキーマ詳細',
  // icon: null,
  component: Schema,
}

export default schemaRoute
